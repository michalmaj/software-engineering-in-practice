[project]
name = "notifier-python"
version = "0.1.0"
description = "Lab 14: the Notifier contract, Python side"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8,<9",
]

[tool.pytest.ini_options]
pythonpath = ["."]
