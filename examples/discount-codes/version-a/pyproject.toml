[project]
name = "discount-codes-version-a"
version = "0.1.0"
description = "Discount codes, version A: coupled into calculate_bill"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8,<9",
]

[tool.pytest.ini_options]
pythonpath = ["."]
